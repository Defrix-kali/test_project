// Placeholder for aspnetcore browser refresh script.
// When running without `dotnet watch`, some templates still try to load
// `_framework/aspnetcore-browser-refresh.js`. Providing this no-op file
// prevents a 404 in the browser console. Safe to remove if you use
// the real hot-reload provided by `dotnet watch`.
(function(){ /* no-op placeholder */ })();
